class Movie {
  title: string;
  price: number;
  year: number;

  constructor(title: string, price: number, year: number) {
    this.title = title;
    this.price = price;
    this.year = year;
  }
}
export default Movie;
